echo "🔍 Проверка переводов (только измененные файлы)..."
node tools/i18n/pre-commit-check.mjs || exit 1

echo ""
echo "🔍 Линтинг кода (измененные проекты)..."
# Пробуем использовать nx affected, если не получается - линтим все проекты
nx affected:lint --base=origin/main 2>/dev/null || \
nx affected:lint --base=main 2>/dev/null || \
pnpm lint:all || exit 1

echo ""
echo "✅ Pre-commit проверки пройдены!"
