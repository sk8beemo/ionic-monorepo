# Архитектурный обзор проекта

**Дата обзора:** 26 января 2026  
**Версия проекта:** 0.0.0  
**Статус:** Базовая структура готова, требуется доработка для production

---

## 📊 Общая оценка

| Критерий | Оценка | Комментарий |
|----------|--------|-------------|
| **Архитектура** | 7/10 | Хорошая база, нужна структуризация |
| **Масштабируемость** | 8/10 | Nx монорепо отлично подходит |
| **Готовность к production** | 5/10 | Нужны критичные улучшения |
| **Поддерживаемость** | 7/10 | Чистый код, но нужна структура |
| **Производительность** | 6/10 | Базовые оптимизации есть, нужны улучшения |

**Итоговая оценка: 6.6/10** - Хорошая основа, требует доработки для production

---

## ✅ Сильные стороны

### 1. Монорепозиторий Nx
- ✅ **Масштабируемость**: Легко добавлять новые приложения и библиотеки
- ✅ **Переиспользование кода**: Общие библиотеки (например, `i18n`)
- ✅ **Единая конфигурация**: Общие настройки TypeScript, ESLint, сборки
- ✅ **Кэширование**: Nx кэширует сборки и тесты

### 2. Модульная структура
- ✅ Разделение `apps/` и `libs/`
- ✅ Библиотека `i18n` вынесена и переиспользуема
- ✅ Четкое разделение ответственности

### 3. Современный стек
- ✅ Angular 21 (последняя версия)
- ✅ Ionic 8.7 (актуальная версия)
- ✅ Vite для разработки (быстрая пересборка)
- ✅ Angular DevKit для production (оптимизация)
- ✅ TypeScript 5.9 (строгая типизация)

### 4. Интернационализация (i18n)
- ✅ Transloco интегрирован
- ✅ Сервис управления языками (`LanguageService`)
- ✅ Сохранение выбора языка в localStorage
- ✅ Поддержка RU/EN

### 5. Структура проекта
```
ionic-monorepo/
├── apps/
│   └── scratch-master-app/          # Основное приложение
├── libs/
│   └── i18n/              # Библиотека локализации
├── nx.json                 # Конфигурация NX
├── package.json            # Зависимости
└── tsconfig.base.json      # Базовые настройки TS
```

---

## ⚠️ Критические пробелы для production

### 1. ❌ Управление состоянием (State Management)
**Проблема:** Нет централизованного state management.

**Рекомендации:**
- **NgRx** - для сложных приложений с большим количеством состояния
- **Akita** - проще в использовании, меньше boilerplate
- **RxJS + Services** - минималистичный подход (рекомендуется начать с этого)

**Структура:**
```
libs/
  └── state/
      ├── auth/            # Состояние авторизации
      ├── user/            # Профиль пользователя
      └── data/            # Бизнес-данные
```

### 2. ❌ HTTP и API слой
**Проблема:** Нет структурированного API слоя.

**Рекомендации:**
```
libs/
  └── api/
      ├── services/
      │   ├── auth.service.ts
      │   ├── user.service.ts
      │   └── api.service.ts (базовый)
      ├── interceptors/
      │   ├── auth.interceptor.ts
      │   ├── error.interceptor.ts
      │   └── loading.interceptor.ts
      ├── models/
      │   └── *.ts (типы/интерфейсы)
      └── guards/
          └── auth.guard.ts
```

### 3. ❌ Структура приложения
**Текущая структура:**
```
app/
  └── pages/
      └── home/
```

**Рекомендуемая структура:**
```
app/
  ├── core/              # Singleton сервисы, guards, interceptors
  │   ├── services/
  │   ├── guards/
  │   └── interceptors/
  ├── shared/            # Переиспользуемые компоненты, директивы, пайпы
  │   ├── components/
  │   ├── directives/
  │   └── pipes/
  ├── features/          # Feature модули
  │   ├── auth/
  │   ├── profile/
  │   └── home/
  └── layout/            # Layout компоненты
      ├── header/
      └── tabs/
```

### 4. ❌ Обработка ошибок
**Проблема:** Нет централизованной обработки ошибок.

**Нужно добавить:**
- Global error handler
- Error interceptor для HTTP
- Toast/Alert сервис для уведомлений
- Retry логика для сетевых запросов

### 5. ❌ Безопасность
**Отсутствует:**
- Auth guards для защищенных роутов
- Token refresh механизм
- Secure storage для токенов (Capacitor Secure Storage)
- HTTPS enforcement

### 6. ⚠️ Производительность
**Частично реализовано:**
- ✅ Vite для быстрой разработки
- ✅ Angular DevKit для production
- ❌ Lazy loading модулей (нет)
- ❌ OnPush change detection (не везде)
- ❌ Virtual scrolling для больших списков
- ❌ Image optimization
- ❌ Code splitting

### 7. ⚠️ Тестирование
**Текущее состояние:**
- ✅ Vitest настроен
- ✅ Playwright настроен (но e2e проект удален)
- ❌ Unit тесты для сервисов (нет)
- ❌ Component тесты (минимальные)
- ❌ Integration тесты (нет)

### 8. ❌ CI/CD
**Отсутствует:**
- GitHub Actions / GitLab CI
- Автоматические сборки
- Тесты в CI
- Автоматический деплой

---

## 🎯 Рекомендуемый план развития

### Фаза 1: Фундамент (1-2 недели)
**Приоритет: КРИТИЧНО**

1. ✅ Реструктуризация папок (`core/`, `shared/`, `features/`)
2. ✅ API слой с базовым сервисом
3. ✅ HTTP interceptors (auth, error, loading)
4. ✅ Error handling (global handler + interceptor)
5. ✅ Toast/Notification сервис

**Результат:** Готова инфраструктура для дальнейшей разработки

---

### Фаза 2: Авторизация (1-2 недели)
**Приоритет: ВЫСОКИЙ**

1. ✅ Auth service + state management (RxJS + BehaviorSubject)
2. ✅ Auth guards для роутов
3. ✅ Login/Register страницы
4. ✅ Token management (storage, refresh)
5. ✅ Secure storage для токенов

**Результат:** Полноценная система авторизации

---

### Фаза 3: Основной функционал (2-4 недели)
**Приоритет: СРЕДНИЙ**

1. ✅ Feature модули (lazy loading)
2. ✅ Основные экраны приложения
3. ✅ Навигация (tabs/menu)
4. ✅ Интеграция с API
5. ✅ Оптимизация производительности

**Результат:** Рабочее приложение с основным функционалом

---

### Фаза 4: Полировка (1-2 недели)
**Приоритет: НИЗКИЙ**

1. ✅ Оптимизация производительности
2. ✅ Улучшение UX/UI
3. ✅ Тестирование (unit, integration, e2e)
4. ✅ Документация
5. ✅ CI/CD настройка

**Результат:** Production-ready приложение

---

## 📋 Детальный чеклист для следующего этапа

### Немедленные действия (Фаза 1)

#### 1. Реструктуризация приложения
- [ ] Создать `apps/scratch-master-app/src/app/core/`
- [ ] Создать `apps/scratch-master-app/src/app/shared/`
- [ ] Создать `apps/scratch-master-app/src/app/features/`
- [ ] Создать `apps/scratch-master-app/src/app/layout/`
- [ ] Переместить существующие компоненты в новую структуру

#### 2. API библиотека
- [ ] Создать `libs/api/` библиотеку
- [ ] Базовый `ApiService` с конфигурацией
- [ ] Типы для API запросов/ответов
- [ ] HTTP interceptors:
  - [ ] `AuthInterceptor` - добавление токенов
  - [ ] `ErrorInterceptor` - обработка ошибок
  - [ ] `LoadingInterceptor` - индикаторы загрузки

#### 3. Error Handling
- [ ] `GlobalErrorHandler` сервис
- [ ] `ErrorInterceptor` для HTTP ошибок
- [ ] `ToastService` для уведомлений
- [ ] Типизированные ошибки

#### 4. Core сервисы
- [ ] `StorageService` (абстракция над localStorage)
- [ ] `NetworkService` (проверка сети)
- [ ] `PlatformService` (определение платформы)

---

## 🔧 Технические детали

### Текущий стек
- **NX**: 22.4.1
- **Angular**: 21.1.1
- **Ionic**: 8.7.17
- **TypeScript**: 5.9.2
- **Vite**: 7.0.0
- **pnpm**: workspace
- **Transloco**: 7.0.0
- **Capacitor**: 7.0.0

### Структура монорепозитория
```
ionic-monorepo/
├── apps/
│   └── scratch-master-app/              # Основное приложение
│       ├── src/
│       │   ├── app/
│       │   │   ├── pages/      # Страницы (нужно реструктурировать)
│       │   │   ├── app-module.ts
│       │   │   └── app.routes.ts
│       │   └── ...
│       └── capacitor.config.ts
├── libs/
│   └── i18n/                   # Библиотека локализации
│       └── src/
│           └── lib/
│               ├── assets/i18n/
│               └── services/
└── ...
```

### Пути импорта (tsconfig.base.json)
```json
{
  "paths": {
    "@ionic-monorepo/i18n": ["libs/i18n/src/index.ts"]
  }
}
```

**Нужно добавить:**
```json
{
  "paths": {
    "@ionic-monorepo/i18n": ["libs/i18n/src/index.ts"],
    "@ionic-monorepo/api": ["libs/api/src/index.ts"],
    "@ionic-monorepo/shared": ["libs/shared/src/index.ts"]
  }
}
```

---

## 🚀 Команды для работы

### Разработка
```bash
pnpm dev              # Запуск dev сервера
pnpm build            # Сборка
pnpm build:prod       # Production сборка
pnpm test             # Unit тесты
pnpm lint             # Линтинг
```

### Capacitor
```bash
pnpm cap:add:ios      # Добавить iOS
pnpm cap:add:android  # Добавить Android
pnpm cap:sync         # Синхронизация
pnpm cap:open:ios     # Открыть в Xcode
pnpm cap:open:android # Открыть в Android Studio
```

### Nx команды
```bash
nx generate @nx/angular:library api --directory=libs/api
nx generate @nx/angular:library shared --directory=libs/shared
nx graph              # Визуализация зависимостей
```

---

## 📝 Примечания

1. **E2E тесты**: Проект `scratch-master-app-e2e` был удален по запросу. При необходимости можно создать новый через `nx generate @nx/playwright:configuration`.

2. **State Management**: Рекомендуется начать с RxJS + Services, при необходимости мигрировать на NgRx/Akita.

3. **Lazy Loading**: После реструктуризации на features, настроить lazy loading для модулей.

4. **Testing**: После создания core сервисов, добавить unit тесты с покрытием >80%.

5. **CI/CD**: Настроить после завершения Фазы 3.

---

## 🎓 Рекомендуемые ресурсы

- [Nx Documentation](https://nx.dev)
- [Ionic Angular Best Practices](https://ionicframework.com/docs/angular/your-first-app)
- [Angular Style Guide](https://angular.io/guide/styleguide)
- [RxJS Patterns](https://rxjs.dev/guide/overview)
- [Capacitor Documentation](https://capacitorjs.com/docs)

---

## 📞 Следующие шаги

При продолжении работы в новом чате, начните с:
1. Реструктуризации приложения (Фаза 1, пункт 1)
2. Создания API библиотеки (Фаза 1, пункт 2)
3. Настройки Error Handling (Фаза 1, пункт 3)

**Команда для начала:**
```bash
# Создать API библиотеку
nx generate @nx/angular:library api --directory=libs/api --importPath=@ionic-monorepo/api
```

---

**Последнее обновление:** 26 января 2026  
**Версия документа:** 1.0
